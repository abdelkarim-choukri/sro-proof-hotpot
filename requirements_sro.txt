# SRO-specific dependencies (additive — do not bump existing pinned versions).
# Install with the Tsinghua mirror:
#   pip install -r requirements_sro.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Core ML
torch>=2.0
transformers>=4.40
tokenizers>=0.15
accelerate>=0.30

# Sentence splitting for [SENT] injection (Phase 5)
nltk>=3.8

# Silver pipeline parsing (Phase 3)
json_repair>=0.25
rouge-score>=0.1.2
python-Levenshtein>=0.23

# Tokenizer/utility
sentencepiece>=0.1.99
protobuf>=3.20

# Splits / CV
scikit-learn>=1.3
numpy>=1.24

# Tests
pytest>=7.4